<h1><%= @book.title %> (<%= @book.pages %> pages)</h1>

<h2>Authors:</h2>

<ul>
  <% @book.authors.each do |author| %>
  <li>
    <%= link_to author.name, author_path(author) %> (b. <%= author.birthyear %>)
    <%= button_to 'Remove Author', author_path(author), method: :delete %>
  </li>
  <% end %>
</ul>
